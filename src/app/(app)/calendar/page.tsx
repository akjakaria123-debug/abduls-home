import { ComingSoon } from '@/components/layout/coming-soon';

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      phase="Coming in Phase 4"
      description="Day, week, and month views of your generated posts — edit, regenerate, approve, or reschedule from here."
    />
  );
}
